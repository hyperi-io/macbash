#Summary
No this is not what you do when Apple increases RAM prices to even more exorbitant levels  
Goals:
- A golang project (keep it simple!)
- For ci pipeline and local testing of bash scripts so they will work reliably on macos (scripts typically developed lon linux)
- checks a bash script for SPECIFICALLY macos compatibility with warns, info and errors (errors is return code 1)
- option to FIX/correct the code to work on Mac